name: Release and Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    outputs:
      released: ${{ steps.semantic.outputs.released }}
      version: ${{ steps.semantic.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Python Semantic Release
        id: semantic
        uses: python-semantic-release/python-semantic-release@v9.8.8
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          root_options: "-vv"

  build-macos:
    name: Build macOS Binary
    runs-on: macos-latest
    needs: release
    if: needs.release.outputs.released == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: v${{ needs.release.outputs.version }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -e .[full]

      - name: Build binary with PyInstaller
        run: |
          pyinstaller espresso-gui.spec

      - name: Verify build
        run: |
          ls -lah dist/
          test -d "dist/Espresso.app" || exit 1
          echo "✅ Espresso.app built successfully"

      - name: Create DMG
        run: |
          brew install create-dmg

          create-dmg \
            --volname "Espresso Installer" \
            --volicon "icons/icon-512.png" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Espresso.app" 175 120 \
            --hide-extension "Espresso.app" \
            --app-drop-link 425 120 \
            --no-internet-enable \
            "dist/Espresso-${{ needs.release.outputs.version }}-macOS.dmg" \
            "dist/Espresso.app" || true

          if [ ! -f "dist/Espresso-${{ needs.release.outputs.version }}-macOS.dmg" ]; then
            echo "create-dmg failed, creating simple DMG..."
            hdiutil create -volname "Espresso" -srcfolder "dist/Espresso.app" -ov -format UDZO "dist/Espresso-${{ needs.release.outputs.version }}-macOS.dmg"
          fi

      - name: Create ZIP archive
        run: |
          cd dist
          zip -r "Espresso-${{ needs.release.outputs.version }}-macOS.zip" Espresso.app
          cd ..

      - name: Upload binaries to release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.release.outputs.version }}
          files: |
            dist/Espresso-*-macOS.dmg
            dist/Espresso-*-macOS.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Homebrew tap update - currently disabled, using manual update via /update-tap
  # To enable: uncomment this job and add HOMEBREW_TAP_TOKEN secret
  # See docs/HOMEBREW_SETUP.md for setup instructions
  #
  # update-homebrew-tap:
  #   name: Update Homebrew Tap
  #   runs-on: ubuntu-latest
  #   needs: [release, build-macos]
  #   if: needs.release.outputs.released == 'true'
  #
  #   steps:
  #     - name: Checkout homebrew-tap repository
  #       uses: actions/checkout@v4
  #       with:
  #         repository: slauger/homebrew-tap
  #         token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
  #         path: homebrew-tap
  #
  #     - name: Download ZIP artifact
  #       run: |
  #         # Download the ZIP file from the release
  #         VERSION="${{ needs.release.outputs.version }}"
  #         wget "https://github.com/slauger/espresso-macos/releases/download/v${VERSION}/Espresso-${VERSION}-macOS.zip" -O espresso.zip
  #
  #     - name: Calculate SHA256
  #       id: sha256
  #       run: |
  #         SHA256=$(shasum -a 256 espresso.zip | awk '{print $1}')
  #         echo "sha256=${SHA256}" >> $GITHUB_OUTPUT
  #         echo "Calculated SHA256: ${SHA256}"
  #
  #     - name: Update Cask file
  #       run: |
  #         VERSION="${{ needs.release.outputs.version }}"
  #         SHA256="${{ steps.sha256.outputs.sha256 }}"
  #
  #         cd homebrew-tap
  #
  #         # Update version and sha256 in the cask file
  #         sed -i "s/version \".*\"/version \"${VERSION}\"/" Casks/espresso.rb
  #         sed -i "s/sha256 \".*\"/sha256 \"${SHA256}\"/" Casks/espresso.rb
  #
  #         # Show the changes
  #         git diff Casks/espresso.rb
  #
  #     - name: Commit and push changes
  #       run: |
  #         cd homebrew-tap
  #         git config user.name "github-actions[bot]"
  #         git config user.email "github-actions[bot]@users.noreply.github.com"
  #
  #         git add Casks/espresso.rb
  #         git commit -m "chore: update espresso to ${{ needs.release.outputs.version }}"
  #         git push

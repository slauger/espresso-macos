<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow Apple Events (for controlling other apps) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Allow microphone access (for audio monitoring) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Allow screen recording (for screen monitoring) -->
    <key>com.apple.security.screen-recording</key>
    <true/>

    <!-- Hardened runtime -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow loading unsigned libraries (needed for some Python libraries) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables (needed for Python runtime) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable library validation (needed for PyInstaller bundles) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
